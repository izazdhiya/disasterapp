<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
<!--    <style name="Base.Theme.DisasterApp" parent="Theme.Material3.DayNight.NoActionBar">-->
<!--        &lt;!&ndash; Customize your dark theme here. &ndash;&gt;-->
<!--        &lt;!&ndash; <item name="colorPrimary">@color/my_dark_primary</item> &ndash;&gt;-->
<!--    </style>-->

    <style name="Base.Theme.DisasterApp" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="AppTheme.TransparentStatusBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <style name="AppSearchView" parent="Widget.AppCompat.SearchView">
        <!-- Customize the color of the text -->
        <item name="android:textColor">@android:color/white</item>

        <!-- Remove the search icon -->
        <item name="searchIcon">@android:color/transparent</item>

        <!-- Remove the underline -->
        <item name="queryBackground">@android:color/transparent</item>
    </style>
</resources>